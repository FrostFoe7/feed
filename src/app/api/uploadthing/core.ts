// This module is deprecated. File uploads are now handled via /api/upload route using AppWrite Storage.
// Keeping this file to prevent import errors from any residual references.
export type OurFileRouter = Record<string, never>;
export const ourFileRouter = {};
